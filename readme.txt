Python Files:
pythonComm.py             Implementation for the PythonComm() class, which is responsible for the communication between Python and the lower layers
gui.py	         	         Implementation for the Gui() class,  which implements a with Tkinter, and using the PythonComm() class
interface.py	         The main executaion file for the Python interface, which uses the above classes